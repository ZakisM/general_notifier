version: '3.8'

services:
  backend:
    image: zakism/general-notifier:latest
    depends_on:
      - splash
    environment:
      - DISCORD_TOKEN=ODUxODgyOTY2NjUyMjg5MDU1.YL-v1g.1fD6H7bLQY5_cEwmaAjs2aVbDDA
    volumes:
      - ./general_notifier_data:/data

  splash:
    image: scrapinghub/splash:master
    ports:
      - "127.0.0.1:8050:8050"
    restart: always
